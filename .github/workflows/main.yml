name: deploiment continu

on:
  push:
     branches:
       - main

jobs:
  test:
     runs-on: ubuntu-latest

     steps:
       - name: Je recupere mon code
        uses: actions/checkout@v4

       - name: Je verifie que ca marche
       run: echo"le deploiment se lance tout seul apres un push"
