name: deploiment continu

on:
  push:
     branches:
       - main

jobs:
  test:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@v4

        - name: Message
          run: echo "Workflow execute avec succes"
